# Code Map - xml-parsing-library

## Repository State

N/A - greenfield; no implementation code exists yet.

## Four-Object Summary

| Object | Current State | Planned Role |
|---|---|---|
| `README.md` | Minimal project title. | User-facing overview, build instructions, and examples. |
| `LICENSE` | MIT license. | License metadata for package consumers. |
| `.gitignore` | Generic compiled artifact ignores. | Must gain CMake/test/coverage artifact patterns when build files are added. |
| `docs/slo/*` | Generated by this planning pass. | Planning contract for architecture, security, and milestones. |

## Exemplar Code To Copy

N/A - greenfield; no brownfield exemplar exists. Milestone 1 must establish project style in `CMakeLists.txt`, `include/xmlparser/xmlparser.h`, and initial tests.

## Anti-Exemplar Code Not To Copy

N/A - no legacy implementation exists. Do not import parser code from third-party projects to avoid license and architecture drift.

## Dangerous Boundaries

- Public header names and namespace must not churn once examples compile.
- DOM ownership and handles must be designed before exposing mutation APIs.
- SAX incremental parser state must be tested across arbitrary chunk boundaries.
- Entity expansion and DTD validation must be bounded from the first implementation.
- Optional external DTD resolution must not become an implicit runtime dependency.
